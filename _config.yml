# Configuración de GitHub Pages/Jekyll para este repositorio
# Evita procesar backend y dependencias en el build estático.

theme: jekyll-theme-primer

exclude:
  - node_modules
  - backend
  - backend/node_modules
  - backend/node_modules/**
